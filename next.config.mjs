/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "developers.kakao.com",
      "lh3.googleusercontent.com",
      "k.kakaocdn.net",
    ],
  },
};

export default nextConfig;
